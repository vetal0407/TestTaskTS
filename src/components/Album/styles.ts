import { StyleSheet } from 'react-native'

const styles = StyleSheet.create({
  headerWrapper: {
    width: '100%',
    marginTop: 20,
    padding: 10,
    flexDirection: 'row',
    alignItems: 'center'
  },
  title: {
    marginLeft: 20
  },
  image: {
    width: 50,
    height: 50
  }
})

export default styles
